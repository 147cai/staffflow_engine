# StaffFlow 排班预测工具 — 使用说明

## 一、工具简介

StaffFlow 读取标准化的 Excel 排班模板，自动生成：
- **各订单排班后的情况表**：各订单各级别预算消耗与剩余预测
- **最新排班表**：每位人员各月排班天数（含借还标注）
- **人员转场日期表**：预测转场时间与原因
- **预警说明**：人员闲置、预算紧张等风险提示

---

## 二、快速开始

### 1. 修改起始日期（每次运行前必做）

打开 `config/run_config.yaml`，将 `start_date` 改为本次排班的起始日期：

```yaml
start_date: "2026.4.1"   # 格式：YYYY.M.D（年.月.日）
end_date:                 # 留空 = 自动推断到所有订单最晚结束月
```

> **示例**
> - `2026.4.1` → 从 2026 年 4 月起排
> - `2026.5.10` → 从 2026 年 5 月起排（5 月按整月计算，日期仅用于定位月份）

### 2. 准备输入文件

将填写好的排班模板 Excel 放到任意路径，记下路径备用。
模板文件示例：`C:\01_NanTian\项目\排班\相关文件\深开排班表生成模板v0.2.xlsx`

### 3. 运行工具

在工具根目录（`staffflow_engine`）下，打开命令行执行：

```bat
conda activate staffflow_engine

python main.py ^
  --input "C:\01_NanTian\项目\排班\相关文件\深开排班表生成模板v0.2.xlsx" ^
  --config-dir "config" ^
  --output-dir "data/output"
```

输出文件自动保存到 `data/output/排班预测_YYYYMMDD_HHMMSS.xlsx`，
命令行会打印完整路径。

---

## 三、配置文件说明

### `config/run_config.yaml` — 每次运行前修改

| 字段 | 格式 | 说明 |
|------|------|------|
| `start_date` | `YYYY.M.D` | 排班起始日期（必填），如 `2026.4.1` |
| `end_date` | `YYYY.M.D` 或留空 | 排班截止月份；留空则自动取所有在途订单最晚结束月 |

### `config/rules.yaml` — 规则参数（通常不需要改）

| 字段 | 默认值 | 说明 |
|------|--------|------|
| `budget.work_days_per_month` | `22` | 标准月工作日数（用于人月换算） |
| `budget.exhaustion_threshold` | `0.05` | 预算耗尽阈值（人月），低于此值视为耗尽 |
| `downgrade.enabled` | `true` | 是否允许高级人员降级使用低级预算 |
| `scoring.continuity_bonus` | `1000` | 留在当前订单的评分奖励（优先不转场） |
| `scoring.urgency_bonus` | `500` | 订单临近到期的额外评分奖励 |

### `config/calendar.yaml` — 每月实际工作日数

列出每个月的实际工作日（含法定假期调整）。新增年份时在此文件补充。

```yaml
2026-04: 20
2026-05: 19
2026-06: 21
...
```

---

## 四、命令行参数

```
python main.py --input <Excel路径> [选项]

必填:
  --input         输入 Excel 文件路径

可选:
  --config-dir    配置文件目录（默认: config）
  --output-dir    输出目录（默认: data/output）
  --start-date    排班起始日期 YYYY.M.D，覆盖 run_config.yaml 中的设置
  --end-date      排班截止月份 YYYY.M.D，覆盖 run_config.yaml 中的设置
  --debug         开启详细日志
```

> `--start-date` / `--end-date` 与 `run_config.yaml` 优先级：
> **命令行参数 > run_config.yaml > 报错**（start_date 未设置时报错退出）

---

## 五、输入 Excel 模板说明

模板包含以下 Sheet（名称固定）：

| Sheet 名称 | 内容 | 关键列 |
|-----------|------|-------|
| 订单清单 | 所有在途订单基本信息 | 编号、订单名称、领域、状态、开始/结束日期 |
| 订单当前工作量 | 各订单各级别已消耗人月 | 编号、级别、已消耗人月、合同工作量（人月） |
| 当前在岗人员清单 | 所有在岗人员基本信息 | 姓名、领域 |
| 当前订单人员清单 | 人员当前所在订单及角色 | 订单编号、姓名、资质级别、进场时间、退场时间 |
| 遴选清单F150159（示例） | 新订单预备人员名单 | 姓名、级别（Sheet 名以 "遴选清单" 开头） |
| 借还配置 | 跨领域借用人员安排 | 姓名、借入订单、发生月份、借用天数 |

> **借还天数跨月自动拆分**：若借用天数超过当月工作日（如 77 天），系统自动按月拆分到后续月份，无需手动处理。

---

## 六、输出 Excel 说明

### Sheet 1：各订单排班后的情况表

| 列名 | 说明 |
|------|------|
| 合同编号 | 留空（可手动填写） |
| 项目编号 | 订单完整编号 |
| 合同名称 | 订单名称（同一订单多行时合并显示） |
| 资质级别 | 高级 / 中级 / 初级 |
| 合同工作量 | 合同总预算（人月） |
| X.X日数据 | 截至模板生成日的历史已消耗（人月）= 录入的 consumed_before |
| 当前考勤缺口 | 合同工作量 - 历史已消耗（负值代表已超出预算） |
| 预测人月 | 本次排班新增消耗（人月） |
| 合同计划考勤完成人月 | 排班后剩余缺口 = 当前考勤缺口 - 预测人月（负值代表预计超） |

不同订单使用不同背景色区分；同一订单的项目编号和合同名称列合并单元格。

### Sheet 2：最新排班表

按订单编号升序排列；每个订单下按姓名排序，组间插入空行；借还人员姓名用**橙色字体**标注。

### Sheet 3：人员转场日期表

记录本次排班中预测会发生转场（含转入新订单和进入闲置）的人员信息及预测日期。

### Sheet 4：预警说明

汇总人员闲置、订单预算紧张等风险，并给出建议。

---

## 七、常见问题

**Q：运行报错"未指定排班起始日期"**
A：打开 `config/run_config.yaml`，确认 `start_date` 已填写且格式正确（如 `"2026.4.1"`）。

**Q：某人员在所有月份都无排班**
A：检查该人员的退场时间（exit_date）是否已过期；或其所在领域与当前所有在途订单领域不匹配（领域壁垒）。

**Q：借用天数 77 天，但只看到部分月份有借还记录**
A：系统自动拆分到后续月份。若某月日历中无数据（`calendar.yaml` 未配置），拆分到该月停止，剩余天数在预警说明中提示。

**Q：预测结果"合同计划考勤完成人月"为负值**
A：负值表示该订单该级别按当前排法会超出合同预算，建议 HR 经理与项目组协商调整人员配置或申请追加预算。

---

## 八、目录结构

```
staffflow_engine/
├── main.py                  # 入口
├── 使用说明.md               # 本文件
├── config/
│   ├── run_config.yaml      # ★ 每次运行前修改（起始日期）
│   ├── rules.yaml           # 排班规则参数
│   ├── calendar.yaml        # 每月实际工作日数
│   └── input_schema.yaml    # Excel 列名映射（一般无需修改）
├── data/
│   └── output/              # 生成的排班预测 Excel
├── logs/                    # 运行日志
└── src/                     # 源代码
```
